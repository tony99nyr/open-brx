/** The server's WSL reachability warning, shared by the mock and its parity test. */
export const WSL_UNREACHABLE_WARNING = "PHONES CANNOT REACH THIS ADDRESS — this looks like WSL2's own private network, not the Windows host's LAN. Pass --advertise <windows-lan-ip> (find it with `ipconfig` on Windows) to put the real address in the QR and mDNS without moving where MC binds, and forward the ports with a netsh portproxy (`netsh interface portproxy add v4tov4 listenaddress=<windows-lan-ip> listenport=8766 connectaddress=<this WSL IP> connectport=8766`, and again for 8765) — the WSL IP changes on every restart, so redo the portproxy each time.";
