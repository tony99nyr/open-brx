# HUD screen review, 2026-09-03

Tony's feedback from walking every Companion HUD state in the STAGE harness (`cd app && npm run ui:stage`,
http://localhost:4190/). Logged as received, then worked 2026-09-03 (same session): every row's status names the fix and the screen-truth step in `app/tools/screens.mjs` that would fail if it regressed (`npm run ui:screens`). Each item: the state it was seen in, what's wrong,
and (once decided) the fix. Status: `open` → `fixed <sha>` / `wontfix <why>` / `moved F<n>` (docs/FOLLOWUPS.md).

| # | State | Issue | Status |
|---|-------|-------|--------|
| 1 | kitted (also every lobby-family screen with the status line) | The ⓘ debug icon (top-right) overlaps the `HOST ✓ · IN SYNC ✓` status line and crowds the BRIEFING button below it. The icon sits on top of the text rather than in its own space. | fixed — the ⓘ lives in a 30px corner box (top/right 4px) and every top-right element keeps a 48px gutter; guarded by screens.mjs invariants (nothing paints under the ⓘ rect) |
| 2 | idle | Tagger list has a horizontal scrollbar: the rows overflow their column to the right (the RSSI bars/number run under the ⓘ icon), so the list scrolls sideways and the ⓘ icon overlaps the first row's signal readout. Rows should fit the column width; no horizontal overflow anywhere. | fixed — root causes: (a) `#frame` was a flex item and SHRANK below 844px on any phone narrower than that, reflowing every screen; now `flex:0 0 844px`; (b) desktop scrollbars ate 15px in every scroller — hidden (`scrollbar-width:none`, touch UI); list is `overflow:hidden auto` |
| 3 | connected | Same ⓘ icon problem: it sits on top of the `GUN CONNECTED ✓ · CONNECTING…` status line, truncating the text under it. One fix for #1/#2/#3: reserve the icon's space in the layout (a fixed top-right gutter) so no status line or list can run under it. | fixed — same as #1 |
| 4 | connected | The join row is misaligned: the URL field + CONNECT button, the SCAN QR button, and the help paragraph each sit at a different vertical position (input low, SCAN QR higher, text top-aligned) and the paragraph is a long ragged block. Align the three on one baseline/centre line and give the row a deliberate layout (input+CONNECT · SCAN QR · one short hint). | fixed — SCAN QR centred in the row (no top margin), input 236px, hint copy shortened; step `#4` asserts the three centres agree within 6px |
| 5 | mc-rejected (kitted + alert pill) | The alert pill (`ASK THE HOST — COULDN'T JOIN`) floats over the plates and clips into the top edge of the loadout plates instead of having its own band. Same for the other chip-bar pills (RECONNECTING…, GUN LINK LOST). Plus the ⓘ icon overlap again on the `CONNECTING… · IN SYNC ✓` line. Give the chip bar a reserved row so alerts never cover content. | fixed — lobby-family screens set `#frame[data-screen=lobby]`, the chip bar becomes a horizontal band at y=98 between the callsign block and the plates (plates moved to 126); step `#5` |
| 6 | setup (smaller screens) | `MISSION CONTROL IS SETTING UP THE GAME` wraps to two lines on smaller phones, orphaning "GAME". Shorten the copy (e.g. `HOST IS SETTING UP` / `SETTING UP THE GAME`) so it stays on one line at every supported size. | fixed — copy is `HOST IS SETTING UP THE GAME`, nowrap; the wrap itself was the #2 frame-shrink bug |
| 7 | mc-rejected / kitted (smaller screens) | On a smaller phone the plates row wraps: the HP/ARMOR plate drops to a new line and lands BEHIND the READY UP button, so `HP 45 · ARMOR 70`, `TDM LOADOUT · #7` and `TAP A SLOT…` are hidden under the footer. The alert pill is also faded behind the plates here. Plates must never wrap: shrink/stack deliberately, and the footer must not overlay content. | fixed — frame-shrink bug (#2) + READY shrunk to 60px (#23); invariants assert plates share one top and never run under the footer |
| 8 | loadout-primary (browser, detail pane) | Detail pane on the right is too narrow for its content: `ASSAULT RIFLE …` title and `MAG 32 · RESERVE 384 · …` line are ellipsised/cut off, and `HITS TO KILL 13 · RELOAD 1.4S` wraps with "1.4S" orphaned. `REAPER #7` header also collides with the ⓘ icon. Either widen the pane, drop the role suffix from the stat line, or stack the stats. | fixed — frame-shrink bug; pane width is 356px at design size; art 84px |
| 9 | loadout-primary (browser, detail pane) | The detail pane doesn't scroll: the weapon description (`The anchor — its frame is the one every other weapon is…`) is clipped by the CLOSE/pick footer with no way to read the rest. The pane needs its own vertical scroll (or the footer must not cover it), and the description should end visibly, not under a button. | fixed — the detail pane scrolls (`overflow:hidden auto`, fade mask) and the description is no longer clamped to two lines; step `#8/#9/#11` |
| 10 | loadout-secondary (browser, list pane) | The filter tabs overflow: `WEAPONS · 13` wraps onto two lines while `PERKS · 4` and `NONE ✓` fit, so the tab row is uneven. The list itself shows a horizontal scrollbar (rows wider than the pane) and the vertical list is cut at the bottom with a scroll track drawn over the last row. Tabs must fit on one line (drop the counts or shorten), rows must fit the pane width, and the list should scroll cleanly to its end. | fixed — filter chips `flex:0 0 auto; white-space:nowrap`, 13px/.14em; list 404px, `overflow:hidden auto`; step `#10` |
| 11 | loadout-picked (browser, detail pane, SMG) | Same pane problems as #8/#9 on a different weapon: `SMG · CLOSE RANGE …` title line ellipsised, `MAG 72 · RESERVE 288 · …` cut off, `RELOAD 2.5S` orphaned onto its own line, description clipped under CLOSE. Confirms the pane layout is wrong for every weapon, not just long names. | fixed — same as #8/#9 |
| 12 | tryout | Elements stacked on top of each other: the two-line status (`TRY-OUT ARMED — FIRE A FEW ROUNDS` / `HOST ✓ · IN SYNC ✓`) runs under the ⓘ icon, the try-out panel's bottom edge is overlapped by the READY UP footer (the `HITS TO KILL 15 · RELOAD 2.5S` line touches the footer), and BRIEFING crowds the status. On smaller devices READY UP, BRIEFING and the hint text wrap badly. The try-out panel + footer need a real vertical layout with no overlap at any size. | fixed — try-out panel at y=126 (below the two-line status + BRIEFING), footer 60px; step `#12` asserts the panel clears both |
| 13 | armed (T-minus, smaller screens) | The countdown overlay's side labels wrap and misalign: `WEAPONS ARMING` breaks onto two lines, `STAND BY` is indented differently from the lines above it, the team chip wraps to `BLUE ·` / `REAPER`, and `TDM · 10:30` wraps too. The big number is fine; the left/right label columns need fixed widths or shorter copy so nothing wraps. | fixed — `white-space:nowrap` on the T-minus labels, chip and mode/clock line; step `#13` counts line boxes |
| 14 | live (top-right cluster, smaller screens) | The top-right cluster is crammed: the match-timer box and the LINK dot overlap, LINK / battery / CAM / ⓘ are jammed with no breathing room, and the `K — D 0 A — ACC —` stat line is far too small to read at arm's length. Rework the header: timer gets its own space, stats get a legible size (or drop ACC on small screens), and the ⓘ icon stops sharing the row. | fixed — header rework: GUN and MC are two dots (`GUN`/`NO GUN`, `MC`), stats 13px labels / 22px numbers, `right:48px` gutter, timer clear of the cluster; step `#14/#20` |
| 15 | live (new: RELOADING moment) | Feature: a full-screen reload animation. When the gun reloads, the HUD takes over the screen for the weapon's reload time (known from the loadout at arm time: catalog `reload_s` per weapon) and shows a RELOADING state that visibly progresses (fill/sweep/countdown) and makes it obvious the player can't fire until it plays out. Same treatment as the existing swap-progress bar, but big. Needs: reload detection from the gun frames (ALCD mag rising / BUT reload button), reload_s carried into the node's weapon view, and the overlay hidden if the gun reports the mag full early. | fixed — engine: reload handle (`$BUT,2,1`) opens `reloading{at,ms}` for the weapon's catalog `reload_s` (1.5s default), cleared when the mag comes back (`$ALCD` up) or `reload_s+600ms`; no reload while dead / full mag / dry reserve. HUD: full-screen RELOADING takeover with weapon name, HOLD FIRE, progress track + seconds left. Engine tests + step `#15`. ⚠ The gun's own reload timing is from `$WEAP` tok (protocol §6) — bench-check that `reload_s` in weapons.json matches what the gun enforces |
| 16 | live-nogun (gun link lost) | Header overlap again with the link-down status: the timer box runs into the `● NO GUN` dot, the `GUN LINK LOST — TAP TO RECONNECT` pill sits awkwardly below the stat line, and the `K 1 ✓MC` stat wraps its check badge. Same header rework as #14, and the pill needs a reserved chip row (#5). | fixed — with #14 and the chip band (#5) |
| 17 | resync (prompt banner) | The resync prompt `GUN RELINKED — PULL THE TRIGGER` wraps mid-phrase ("PULL THE" / "TRIGGER"), which reads badly in all caps. Make the banner wide enough for the longest prompt on one line, or split it deliberately into a small label (`GUN RELINKED`) over a big instruction (`PULL THE TRIGGER`). | fixed — prompt is a small `GUN RELINKED` label over a 26px instruction, nowrap; step `#17` |
| 18 | redeploy (moment, smaller screens) | The REDEPLOYED word is clipped at the right edge on smaller screens (`REDEPLOYE`). The headline needs to scale to the available width (fit-text / clamp) rather than a fixed size; the team panel on the left could also give up width. | fixed — frame-shrink bug + `.r` bounded to `right:24px`, headline nowrap; step `#18/#27` asserts it fits |
| 19 | live-kill (KILL CONFIRMED moment) | The kill confirm is drawn on top of the live HUD and it looks janky: `+1 ELIMINATION` / `KILL CONFIRMED` collide with the header (callsign, LINK, stats), and the `VIPER DOWN · CONFIRMED BY MISSION CONTROL` strip overprints the ammo bar and weapon name. Redesign as a takeover: fade/dim the live HUD, play the kill animation on a clean full screen (like DOWN / REDEPLOYED do), then fade the HUD back. Same treatment should be considered for the other transient moments (hit, gain). | fixed — KILL CONFIRMED is a takeover: near-opaque backdrop dims the HUD, content centred (was at 20%), KILL 96px, fades out instead of collapsing; step `#19` |
| 20 | live (after a score update) | The per-stat `✓MC` sync badges (`K 4 ✓MC · D 0 · A 1 ✓MC · ACC 0% ✓MC`) clutter the stat line into an unreadable mess. Drop the per-stat badges: sync is one fact, show it once (a single small indicator on the stat line or fold it into the LINK/MC status), and keep the stats as plain numbers. | fixed — per-stat ✓MC badges removed everywhere (live + result); sync is the MC dot in the header |
| 21 | result (ACCURACY tile) | Accuracy has only ever shown 0% or nonsense in real games. How it's built: shots = the shooter's own node counting mag decrements while live; hits = counted at MC from the VICTIMS' `hit_taken` facts, attributed by shooter number. So it's only right when every victim's phone is enrolled, in range and synced — one phone-less gun or an out-of-range victim and hits are missing while shots are not, which reads as 0%. Decide: (a) hide the tile unless MC has hits for this player from ≥1 synced victim, (b) drop it from the result screen and keep it in the MC recap only, or (c) bench-verify attribution (shooter_num in $HIR ↔ player_num) before trusting it at all. Also the 5-tile row is cramped on small screens. | fixed (option a) — ACCURACY shows only once MC has counted ≥1 hit for this player AND ≥10 shots went out, else `—` (live: the ACC stat is omitted); contract note added to contracts.md. Open: bench-verify shooter-number attribution (F: see FOLLOWUPS) |
| 22 | over (MATCH COMPLETE, smaller screens) | Same wrap-and-overlap family as #7: the footer `MATCH COMPLETE — READY FOR NEXT` wraps to two lines and paints over the HP/ARMOR plate that dropped below the plates row (`HP 45 · ARMOR 70` faintly visible behind it); `MAG 32 · RESERVE 192` and `ALT-FIRE DOES NOTHING` wrap inside the plates; the `D 0 · K 3` recap top-right collides with the ⓘ icon. Shorten the footer copy (e.g. `MATCH COMPLETE`) and fix the plates row so it never wraps under the footer. | fixed — copy `MATCH COMPLETE` / `READY ✓`, frame-shrink bug, READY size; step `#22` |
| 23 | kitted / kitted-ready (READY UP footer) | The READY UP / READY ✓ button is far too large: it eats a third of the screen height. Cut it down substantially (still a comfortable tap target, ~56–64 px tall), which also frees the room that #7/#12/#22 need for the plates. | fixed — READY is 60px / 26px type (wait variant 20px) |
| 24 | night theme (kitted / lobby) | With the night theme on, the PRIMARY / SECONDARY loadout plates don't show (the plate backgrounds go transparent and the art/labels vanish into the black), so the player can't see or change their kit at night. Night should dim, not hide: keep the plates visible in the red palette. | fixed — night plates get `#0a0303` fill + `#3a1414` edge instead of transparent; step `#24` |
| 25 | down (respawn screen) | Drop the ghosted health/ammo numbers behind the DOWN overlay (they're 00 and meaningless while dead). Use the dead time the way games use the Tab scoreboard: show a small match recap under the redeploy countdown — your K/D/A, team scores, who's leading, time left. Needs MC to push a scoreboard snapshot (the `score` message could carry team totals + top lines) or the node to hold the last one it saw. | fixed — ghost numbers gone; recap row: TIME LEFT, LIVES LEFT (when the config caps lives) or DEATHS, YOU n K, team chips with scores, CAP. MC now sends `score.board {teams:[{team_id,name,score}], cap}` (state.py `_score_board`; FFA = top three players); older MCs → local CAP only |
| 26 | down (respawn screen, detail for #25) | The recap must include the match's live limits: time left on the clock, respawns/lives remaining for this player (when the mode caps them), and distance to the score/frag cap (e.g. `BLUE 18 · YELLOW 21 · CAP 25`). Time left and lives are known on the node already (config + local deaths); the score-cap line needs the team totals from MC (#25). | fixed — part of #25 (time left + lives/deaths + cap) |
| 27 | redeploy (moment) | REDEPLOYED should show what you're going back in with: primary, secondary/perk (name + art, like the kit plates) under `WEAPONS HOT`, replacing the bare `45 · 70 · MAG FULL` line. And the animation should be much more exciting than the current wipe — a real spawn-in beat (sweep/flash/scale-in, sound cue already exists). | fixed — REDEPLOYED lists the kit (PRIMARY / SECONDARY or PERK with art), a light beam sweep + slam-in + whiteout, 1.7s on screen; step `#18/#27`. Lives cap: config has no `respawn.lives` yet — the row falls back to DEATHS until MC exposes one |
| 28 | tryout | The try-out panel was a square box between slanted DONE and READY UP buttons and the slanted plates. | fixed — the panel is skewed like the plates (`skewX(-8deg)`, children unskewed, wider side padding so the art stays inside); step `#28` |
| 29 | live (header stats) | `K — D 0 A —` is noise before anything has happened. | fixed — a stat appears only once it is non-zero (K/D/A), ACC only per #21; the row is empty at spawn; step `#29` |
| 30 | down (recap row) | The first recap was one jumbled line; `YOU 3 K` was awkward and the groups ran together. | fixed — three labelled tiles: TIME LEFT · team chips with `FIRST TO 25` under them (your team outlined) · YOU `3 KILLS · 1 DEATH` (+ lives left when capped). **Team totals and kills are MC data, so they show only while the MC link is up**; off-link the recap is local-only: TIME LEFT · SCORE CAP · YOU `n DEATHS · n SHOTS`; step `#25/#26` covers both |
| 31 | redeploy | The blue wipe and the white slash ran into the REDEPLOYED text, and on Tony's browser the headline still ran off the right edge. | fixed — text column starts at 55% (clear of the skewed slash at any height), and the headline is fitted to its column at render time (shrinks from 56px until it fits) so platform font metrics can't clip it; step `#18/#27` asserts both |
| 32 | live-mclost | Playing the whole match out of MC range is expected, so `RECONNECTING TO MISSION CONTROL…` mid-match is nagging. | fixed — live: the MC dot turns amber and nothing else; tapping the MC label toggles a detail pill (`OUT OF MISSION CONTROL RANGE — SCORES SYNC WHEN YOU ARE BACK`). Kitted/lobby keep the reconnecting pill (MC is required there). Harness: the `reload` button now pulls the handle and refills after `reload_s`, so the takeover plays; steps `#32` / `#32b` |
| 33 | live-reload / down / plates / harness (browser-breaker pass) | (1) chip pills drew over the RELOADING takeover (`WEAPONS HOT` beside `HOLD FIRE`); (2) a reload pulled just before death carried into the next life; (3) DOWN counted down in scanner / no-respawn modes; (4) `MAG 32 ·` still wrapped with a dangling separator; (5) harness `&team=red` rendered green (wrong tids). | fixed — (1) `#frame[data-takeover=reload]` hides the chip bar; (2) `_death`/`_revive` clear `reloading`; (3) `respawnIn` only for auto respawn, DOWN shows a glyph + the mode's instruction instead of a number; (4) the ammo line is a nowrap span; (5) demo tids match MC (red 0, green 3). Steps `breaker-1…5` |
| 34 | polish pass (correctness + UX lenses) | (1) demo could inherit a real session persisted on the same origin (reset ran after the engine's constructor restore); (2) reduced-motion made the whiteout a harder pop; (3) whiteout not rate-limited under a kill-confirm burst; (4) night DOWN team/KILLED BY chips were daylight-bright; (5) recap labels 10px; (6) `ALT-FIRE DOES NOTHING` / `GOES TO YOUR GUN AT ARM TIME` still wrapped; (7) last match's score showed on the first DOWN of the next; (8) A/ACC values not patched live; (9) a dry-reserve pull opened RELOADING; (10) hit/kill moments replayed late after a reload; (11) night had no off-range signal at all; (12) CONNECT/SCAN QR under 44px; (13) the range pill never reset. | fixed — engine built with `storage:null` under `?demo`; `.whiteout{display:none}` under reduced motion; `_flash` ≤2/s; night override for the chips; labels 12px; copy `NO ALT-FIRE` / `SET AT ARM TIME` + nowrap; score reset on a new match_id; `_patch` sets A/ACC; `reserve > 0` guard; moments consumed under the takeover; night shows the dim pill; 44px `::after` rows; `mcPill` resets when bound. Steps `ux-1…4`, `persisted`; engine tests |
| 35 | polish pass 2 (correctness lens) | (1) dying mid-reload left the chip bar hidden for the whole DOWN screen (`GUN LINK LOST` invisible when it mattered); (2) perk subtitles / `NO SECONDARY THIS GAME` / `MAG 300 · RESERVE 600` would ellipsize on the 134px plate line; (3) harness messages were clobbered by the log tick; (4) the welcome score row lacked `board` after a phone swap. | fixed — takeover flag tracked at the top of `_moments` on every render; plate copy `NO SECONDARY` / `FIXED BY THE HOST` / perk effect without the `PERK ·` prefix (the slot label reads `PERK`), wider subtitle line; harness gets its own message line; `welcome.node.score` carries `board`. New stage `kitted-perk`; steps `pass2-1`, `pass2-2` |
| 36 | polish pass 2 (UX lens) | (1) the ▣ scanner glyph on DOWN painted through `GO TO A RESPAWN SCANNER`; (2) a reload pulled just before the gun link dropped kept the RELOADING takeover (and hid `GUN LINK LOST`) for `reload_s`+grace; (3) harness: the shooter's team in `$HIR` was hardcoded, so `&team=yellow` showed a yellow KILLED BY chip. | fixed — glyph 96px/line-height 1; `onBleDropped` clears the reload and the HUD gates the takeover on `bleUp`; demo derives the shooter tid from the foe team. Steps `pass2-3`, `pass2-4` |
| 37 | suite audit (revert-variant pass) | (1) hits/kills were swallowed under the RELOADING takeover; (2) the easy-reload perk's ALT-as-reload never opened the takeover; (3) the description's last line sat under the pane's fade; (4) the chip band landed on content in the loadout browser / briefing / result; (5) the diagnostics panel lost its scrollbars; (6) steps #17/#20/#22 were tautological, the two-pill band was unguarded. | fixed — moments append above the takeover; `_altPressed` with one weapon calls `_reloadPulled`; pane padding beyond the fade; `data-screen` = `lo` (band above the action bar) / `panel` (top strip) for those screens; `#diag` keeps scrollbars; steps sharpened + `audit-1…4` |
| 38 | polish pass 3 (final) | A reload pulled during the resync protocol opened the takeover over the resync prompt; `_endLocal` left `reloading` set (engine state only); harness message line never cleared; suite steps could pass on a stage that failed to load or on hidden text. | fixed — `_reloadPulled` returns during resync; `_endLocal` clears it; harness clears its line on load; every stage load asserts the stage was reached and `oneLine` requires visible text. Step `pass3-1`; engine test. Loop closed at three passes with no Critical/High outstanding |
| 39 | live-reload | `HOLD FIRE` on the RELOADING takeover is redundant: the gun physically cannot fire while reloading. | fixed — the subline is just the weapon name; step `#15` updated |
| 40 | live (new: SWITCHING takeover) | Weapon switch needs the same treatment as reload: a takeover showing which weapon you're switching to and which is now active; you can't shoot mid-switch. | built — `SWITCHING` takeover with STOWING → DRAWING tiles (art + name) and a progress track over the swap window; flips to an `ACTIVE ✓` confirm when the next shot reports the new slot (`CONFIRMED BY YOUR GUN`) or when the window expires (`READY`, assumed — see F4: the swap has never been timed; `SWITCH_MAX_MS` 1.2s is a guess). Replaces the old centre "ALT" indicator. Cancelled by death / link drop. Stage `live-switch`, harness `switch weapon`; step `#40`; engine tests |
| 41 | live (new: game-event ALERT banner, contracts A11.4) | MC pushes named game events (lead taken/lost, next kill wins, bomb planted/defused/detonated, point/hill/flag, VIP, clock callouts); the engine sets `moment{kind:'alert'}` — no visual existed. | built — full-width banner at y 132: a family kicker (`OBJECTIVE` team colour · `CLOCK` amber · `ALERT` red · `MATCH` glow) + the text MC chose, 2.2 s then fades; renders at night (dim). Stage `live-alert`, harness buttons; steps `#41`/`#41b` |
| 42 | live-kill (new: medal stack, A11.4) | A kill can carry Halo-style medals (`data.medals`), announced 2 s apart; nothing showed them. | built — badges under KILL CONFIRMED land one at a time 2 s apart (first blood red, sprees amber, others glow) and the takeover holds `1.8 s + 2 s × (n−1)` to match the audio. Stage `live-medals`, harness `kill + medals`; step `#42` |
