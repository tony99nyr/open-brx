# Bench plan: the 2026-09-25 sitting

The steps live in [`bench-2026-09-24.md`](bench-2026-09-24.md) (the runbook) and
[`audio-queue-scenarios.md`](audio-queue-scenarios.md) (the audio bench plan). This page gives the ORDER only, so each
setup happens once and 0.4.12 can be cut as early as possible. The method is the
[`bench-session` skill](../.claude/skills/bench-session/SKILL.md): the agent drives the tools, Tony handles the
hardware and types `1` when a step is done, every result has a control first, bench audio is `$VOL,65`.

**What gates 0.4.12:** A4 (the `$*` parser reset; F341's fix sends `$*`, so no APK may carry it until A4 passes), and
F347's two answers (the hum's restart delay after `$PLAYX,0`, which the 0.4.12 announcer depends on; and which `$PSET`
t23 value stops the shield hum blocking the audio queue). Sitting A answers all three with one gun and no phones.

## Sitting plan

| sitting | kit | steps, in order | min | unblocks |
|---|---|---|---|---|
| **A. Desk, one gun, no phones** | 1 gun and headset, the laptop (MCP and `raw-bytes`) | Block 0 steps 1-3 (preflight); Block 2 step 4 (**A4**); Block 2 step 6 lines 1-4 (**F341**); Block 10 step 4 = audio steps setup, 1, 2, 3, 4, 5 (**F347**); 4.19 parts 1-3 (the recharge sounds) | 55 | **the 0.4.12 cut**: A4 → F341 ships; the t23 value → brx5's compile fix; the restart delay → brx5's rule B |
| **STOP POINT 1** | | Send the results to brx1 (A4, F341, F347). The developers cut 0.4.12 while sitting B runs. | | |
| **B. The Stick, one phone** | the StickS3 (flashed from `main`), 1 gun and headset, the grey Pixel on a `main` build (`cd app && npm run android:install`; the install wipes its app data), MC with `--powerups`, the laptop | Block 10 step 1 (**F332** side-button lock); Block 10 step 2 (**the Stick pickup**, online then offline); Block 10 step 3 (the Stick's hearing; the revive part optional); Block 9 steps 3 (hill by presence), 4 (the owner survives a restart), 6 (the A58 lock end to end), 7 (scan coexistence, during step 3) | 45 | the Stick MVP: pickup online and offline, the tamper lock, the hill |
| **STOP POINT 2** | | Install 0.4.12 on both Pixels (release-signed over the `main` debug build needs an uninstall on the grey Pixel). | 5 | |
| **C. Two guns, both phones, a real fight** | 2 guns and headsets, both Pixels on 0.4.12, MC with `--powerups`, a phone or Stick powerup station, the laptop | 4.0 (**the 0.4.12 release loop**, day and night); 4.18 (a Shields life starts at 105); 3.4 (**the heavy on the trigger and SELECT**); 3.5 (**the overshield grant and a hit during it**); then one real Shields fight of 5 min for **the announcer and medal audio** (kill confirm, first blood, double kill, lead change, hill if KOTH) and a watch for a wrong IR magnitude (7.11's 22 read as 28); 4.3 (**the AR feel at heavy 45**); 3.2's Burst Rifle part (**the gap at 540**: 20 rapid pulls, the shortest gap) | 75 | 0.4.12 shipped to players; S58 powerups; F308 closed |

**MUST total: about 3 h**, in three setups. Stop at any stop point if time runs out: the order is the priority.

## Notes for the agent

- **Sitting A needs no phone on the gun.** Force-stop the app on every Pixel first (`adb -s <phone> shell am force-stop
  com.openbrx.companion`); one central at a time.
- **F347 decides two things.** Report the t23 table row that matched (A10 / EMPTY / N1A) and the restart delay to brx5
  and brx1 at stop point 1. Do not let anyone ship a t23 change before step 2 of the audio plan has run.
- **Block 9 step 1 (mDNS) is untestable on this laptop**: MC runs in WSL behind a Windows portproxy, so its mDNS does
  not reach the LAN. Skip it here; it needs MC on a machine on the LAN directly (the MacBook).
- **Block 9 steps 2 and 5** (the revive count, the pickup) are replaced by Block 10 steps 2 and 3 (Tony, 2026-09-24:
  "I dont really care how many revives there are or for animating it... but we do need to know that someone got the
  pickup").
- **7.11 passed on 2026-09-24** (10/10 at about 2 m). Sitting C only watches for the in-fight corruption; re-run 7.11
  at 5 m only if a wrong magnitude shows up.
- **The Burst Rifle gap** is an upper bound from hand pulls: the gun fires the next burst on the first pull after the
  delay. Report the shortest gap over 20 pulls and say it is a bound.
- **4.3 at heavy 45** needs the balance change in the build (check `weapons.json`'s `assault_rifle` recoil row in the
  0.4.12 tag before the step).

## LATER, grouped by setup

- **One phone, the Stick:** Block 9 step 1 (mDNS, on the MacBook), step 8 (MUSTER; covered by Block 10 step 2(b)).
- **Two guns, both phones:** 4.13 (the phone KOTH hill), 4.14, 4.15 (F340, Location off), 4.16, 4.17, 4.19 part 4,
  1.5 (a second phone's scan during a headset join), the AR hit rate (Block 10 step 5, if the F308 question reopens).
- **Outdoors:** Block 5 (F275), 7.11 at range.
- **In Tony's house:** Block 6.
