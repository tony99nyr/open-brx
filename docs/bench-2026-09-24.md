# Bench runbook: the 2026-09-24 sitting

The ordered bench runbook. Each step gives the command, the pass or fail rule, and what to log. The method is the
[`bench-session` skill](../.claude/skills/bench-session/SKILL.md): the agent drives the tools, Tony handles the
hardware and types `1` when a step is done, and every result has a control, run first.

## Sitting plan: start here

Each setup happens once. The MUST sittings gate play and the 0.4.6 build; stop after them if time is short. The
blocks below hold every step; this plan only orders them. Minutes exclude carrying kit between rooms.

**MUST (about 3 h 45 min in three setups)**

| sitting | kit | steps, in order | min | unblocks |
|---|---|---|---|---|
| A. Desk, one gun | 1 gun and headset, laptop, both Pixels | Block 0; Block 1 (1.3 only if 1.2 loops; read its bugreports with `btlink`); Block 3 | 80 | F297's first numbers; whether F293's loop is the phone; F308's release order; the Shotgun, Desert Eagle and Burst Rifle cadences the balance rules assume |
| B. Two guns and MC | 2 guns and headsets, both Pixels, MC, laptop, the ESP32 receiver | Block 4.0 (the 0.4.6 loop); Block 4.3 (the AR ladder's feel); Block 7 steps 1-6 and 8 (S57, F312) | 75 | the 0.4.6 publish; F308 closes; S57 ships; F312's capture rule |
| C. Outdoors | 2 guns and headsets, the rig, a laptop | Block 5 (F275); Block 7 step 7 (callout range) | 70 | the headset word's reach, which sets the close band for R4 and R8, and the callout range |

**STOP POINT.** Everything below is LATER: it improves the game but does not gate play or 0.4.6.

**LATER, grouped by the same setups**

| setup | steps | min |
|---|---|---|
| Desk, one gun | Block 2 (the screamers transport steps) | 45 |
| Two guns (MC for the Block 4 steps) | Block 2b (native-firmware captures; H1-H3 can ride sitting B's kills); Block 4.1, 4.2, 4.4-4.10 (includes F315's 4.10); Block 7 steps 9-10 | 70 + 20 optional; 50; 10 |
| Tony's house | Block 6 (S48, the house grid; 6.0 needs a scope) | 60 + 15 optional |
| The M5Stick | F314 with the [`m5stick-bench` skill](../.claude/skills/m5stick-bench/SKILL.md) and [`bench-sticks3-2026-09-23.md`](bench-sticks3-2026-09-23.md) | as that sheet says |

## Blocks

| block | what | rows | time | kit |
|---|---|---|---|---|
| 0 | preflight | - | 15 min | everything |
| 1 | connect reliability | F297, F293 | 45 min | 1 gun and headset, laptop, both Pixels |
| 2 | screamers transport | F269, F270, F272 | 45 min | 1 gun and headset, laptop |
| 2b | the native kill word and the firmware readings | F320, F321, F322, R4 | 70 min, plus 20 optional | 2 guns and headsets, laptop, the rig, a camera |
| 3 | trigger order and fire intervals | F308, F291 | 20 min | 1 gun, laptop |
| 4 | the 0.4.6 loop and one real match | 0.4.6, S56, F320, F308, F291, F292, F298, F296, F309, F311, F310, F315 | 80 min | 2 guns, both Pixels, MC, the ESP32 receiver |
| 5 | outdoor range ladder | F275 | 60 min | 2 guns, outdoor, the rig |
| 6 | super indoor, in Tony's house | S48, F198 | 60 min, plus 15 optional | 2 guns and headsets, laptop; a scope for 6.0 |
| 7 | the IR callout bus | S57, F312 | 55 min | 2 guns, laptop, the ESP32 receiver |

The sitting plan above decides the order. Block 2b is self-contained, so it can move sittings: move its row, nothing
else. Block 5 needs outdoor space. Block 6 runs in Tony's house, not
at the bench. Block 7 fits any indoor sitting with time left; its step 7 needs 30 m.

## Conventions

- **The environment file.** The agent's shell keeps no variables between commands, so every command below starts
  with `source ~/bench-0924.env &&`. Block 0 writes the file. `PY` is Windows Python, the only one with Bluetooth;
  `WPY` is WSL Python, for frame generation only.
- Run every command from the main checkout, `/home/tony/gitrepos/battlecompany`. The Windows venv is an editable
  install of that checkout's `mcp/`, so it runs whatever that checkout holds. Arguments go on the command line
  only: environment variables do not cross from WSL to Windows.
- **Single-quote every frame** in a `raw-bytes` argument. In double quotes the shell expands `$AMMO` to nothing.
- One central at a time: `raw-bytes` and `connect-metrics` open their own link, so disconnect the MCP session (and
  keep the phones off that gun) before each one.
- Bench audio is `$VOL,65` (every `armgen.py` line below passes `volume=65`). Never end a block on a bare `$CLEAR`
  (F11). Power-cycle the gun at the end of a block and disconnect the MCP session.
- A **LOCK-UP**, a **LINK DROP** and a **BAD FRAME** are defined in the screamers sheet ("Rules for every session").
  On a lock-up, the agent connects with the MCP tool and sends `$PLAYX,0,*`, then `$HLED,,6,*`, then `$STOP,*` with
  `send`, notes which got through, then Tony power-cycles the gun and headset, and the gun is re-armed.
- **Reading a phone.** `$PY mcp/tools/webview_eval.py "<js>" --serial <phone ip:port>`. One call takes about a
  second, so read AFTER the action, not during it. `window.brx.link.frames` is the last 60 frames in and out, each
  `{t, dir, f}` with `t` in ms; `window.brx.log` lines carry `[HH:MM:SS]`, good to about 1 s.

## Block 0: preflight (15 min)

1. **Update the main checkout.** Blocks 1 to 4 need `raw-bytes`, `connect-metrics` and S56, which are on `main`.
   The main checkout is shared with other sessions: check `git -C /home/tony/gitrepos/battlecompany status --short`
   first, then `git -C /home/tony/gitrepos/battlecompany pull --ff-only`. Confirm the tools are current: `$PY -m brx_mcp raw-bytes` with no arguments must print a usage that
   names `--phone-pacing`, and `$PY -m brx_mcp connect-metrics` must print its usage. If `--phone-pacing` is missing,
   stop: the paced controls in Block 2 cannot run.
2. **Write the environment file** (outside the repo):

   ```
   cat > ~/bench-0924.env <<'EOF'
   PY=/mnt/c/Users/Tony/.brx-mcp/venv/Scripts/python.exe
   WPY=/home/tony/gitrepos/battlecompany/.venv/bin/python
   GUN='AA:BB:CC:DD:EE:FF'   # replace with the gun's address from: $PY -m brx_mcp scan
   A7='$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$PLAY,U37,3,10,,,,,*$QUERY,*'
   WEAP32=$($WPY /home/tony/gitrepos/battlecompany/mcp/tools/armgen.py 1 5 ar 2>/dev/null | grep '^\$WEAP,0,')
   WEAP30=$($WPY /home/tony/gitrepos/battlecompany/mcp/tools/armgen.py 1 5 ar t16=30 t39=30 2>/dev/null | grep '^\$WEAP,0,')
   EOF
   ```

   Paste it without the list indent, so the closing `EOF` starts its line. Check:
   `source ~/bench-0924.env && echo ${#WEAP32} ${#WEAP30}` prints `111 111`.
3. The preflight in [`gotchas.md`](gotchas.md) ("Before a bench session"): kill stale `brx_mcp` processes, then
   power-cycle the gun and the headset.
4. **Both Pixels.** Re-enable wireless debugging and `adb connect` each one, in both the WSL `adb` (install,
   bugreport) and the Windows `adb.exe` (`webview_eval.py` uses it); they are separate servers. Block 4 needs S56, which is newer than
   the published 0.4.5: build and install from the updated checkout (`cd app && npm run android:install`, once per
   phone). Log `$PY mcp/tools/webview_eval.py "window.brx.APP_VER" --serial <phone>` for each.
5. **The ESP32 rig** (Blocks 2b, 4.2 and 7): plug in board A (the receiver) and board B (the emitter), and check
   they enumerate as COM7 and COM8. Keep a phone camera to hand for Block 2b.
6. **What landed today**, for the gated steps:
   - The Shotgun's fire interval: `source ~/bench-0924.env && cd mcp && $WPY -c "from brx_mcp.mc.compile import WeaponCatalog as C; print(C().fire_ms('shotgun'))"`.
     800 is today's value; 700 means the new cadence landed.
   - F309: `grep -c setConnectionType app/src/transport/transport.js` is 1 or more after the step 1 pull (it
     landed on `origin/main` as `57d6e0d7`). If it is 0, the pull did not happen: skip 4.8.

## Block 1: connect reliability, P0 (45 min)

The question: how long does a cold connect take, how often does the first attempt work, and does the headset drop
in the first 30 s? The laptop run is the control: it has no Android Bluetooth stack in the way.

**1.1 Laptop, 10 cold connects (F297).** Tony runs this in his own WSL terminal, because it waits for Enter:
`source ~/bench-0924.env && $PY -m brx_mcp connect-metrics $GUN --runs 10`
Before each run it asks him to power-cycle the gun and the headset, then press Enter.
- **Log:** the summary (first-attempt success rate, link time median, p90 and max, headset drops, never-linked
  count, errors) and the JSONL path it prints.
- **Pass or fail:** no bar yet; this is the first measurement. Flag any run with a headset or BLE drop inside 30 s.

**1.2 Each Pixel, 5 cold connects (F297).** For each try: power-cycle the gun and the headset, wait for the headset
to settle, then Tony taps the gun in the picker and starts a stopwatch; he stops it at "linked" on the HUD. Then:
`source ~/bench-0924.env && $PY mcp/tools/webview_eval.py "window.brx.log.slice(-40)" --serial <phone>`
- **Log per try:** the stopwatch time; the "connecting to", "connect i/5 failed after N ms" and "gun … linked"
  lines; whether "*** gun disconnected ***" or "gun keeps dropping" appears within 30 s of the link.
- **Pass or fail:** no bar yet. A phone that drops while the laptop in 1.1 did not points F293 at the phone.

**1.3 Only if 1.2 shows the link loop (F293).** One variable at a time, A/B/A:
1. **A:** on the looping phone, turn on Developer options, set "Bluetooth HCI snoop log" to **Enabled** (not
   Filtered, which cuts the payloads short), then toggle Bluetooth off and on. Reproduce the loop for about 60 s, then `adb -s <phone> bugreport bugreport-f293-a1.zip`. Keep the phone
   still for 5 to 10 min while it writes.
2. **B:** turn off Fast Pair and Nearby scanning (Settings, Google, Devices and sharing, Devices, "Scan for nearby
   devices" off). Power-cycle the headset, connect again, watch 60 s, and take `bugreport-f293-b.zip`.
3. **A again:** turn scanning back on and repeat step 2's connect and bugreport (`bugreport-f293-a2.zip`).
- **Log:** loop yes or no in each run, each bugreport file name, and the time of each drop from the phone log.
- **Read each bugreport** with brx5's tool (on `main` once `lane-brx5/f293-phone` lands; until then
  `python -m brx_mcp.btsnoop` gives the ATT traffic only):
  `source ~/bench-0924.env && $PY -m brx_mcp.btlink bugreport-f293-a1.zip --gun $GUN --frames`.
  It ends with one DROP line per drop and a tally. Each is a reading, not a verdict: `gun-host` drops mean the gun
  ended the link (check the last gun frame and the headset LED); `link-loss` means a supervision timeout (check the
  parameter, data-length and MTU lines just before); `phone-host` means our app or Android sent the disconnect; a
  connect to a peer that is not the gun means the phone is touching the headset.
- **Pass or fail:** the question is answered only if B differs from both A runs.

## Block 2: screamers transport (45 min)

One gun, the laptop, `raw-bytes`. The step definitions are screamers A4, A7, A7b, A7c and A8.

**Arm.** The agent connects with the MCP tool, sends the output of `$WPY mcp/tools/armgen.py 1 5 ar volume=65` with
`send_batch`, checks one trigger pull gives an `$ALCD`, then disconnects the MCP session.

**Arguments, pass rules and the magazine read** are the screamers sheet's table ("Running the split and zero-gap
steps"): that table is canonical. Every line here is
`source ~/bench-0924.env && $PY -m brx_mcp raw-bytes $GUN <arguments from the sheet>`; `$A7`, `$WEAP32` and `$WEAP30`
come from the env file. The agent tells Tony when to fire: about 2 s after the tool prints "FIRE NOW".

Order, each control first, then three runs of each variant:

1. **A7c**, the receive-ring boundary: the control at 146 and 147, then zero gap alternating 146 and 147. Log `PONG=`
   and the liveness line per run.
2. **A7**, short-frame burst: the control as shipped, zero gap, block pause. Log `QUERY=` per run and any LOCK-UP.
3. **A8**, long-frame burst: the control as shipped, zero gap, with response. Log the `$ALCD` count and the last
   `$ALCD` after the last write, per run.
4. **A4**, the stale-token parser reset (levers §25): the two-frame control three times, then no reset and reset
   alternating. Log the last `$ALCD` and the count per run.
5. **A7b**, a split frame across a gap: 60 ms and 2 s, three runs each. The A4 control is also this step's control.

End the block with a power-cycle of the gun and the headset.

## Block 2b: the native kill word and the firmware readings (R4; 70 min, plus 20 optional)

Codex's native capture plan and the short R4 checks, in one block. Block 2b needs no state from Block 2: it arms,
restores and power-cycles on its own. Every firmware claim below is a disassembly reading, not a bench fact
([`firmware-image-research-plan.md`](firmware-image-research-plan.md), "2026-09-23 desk findings"). Each step marks
its reading CONFIRMED, REFUTED or INCONCLUSIVE.

**Kit.** Two v4.32 guns with their paired headsets, the laptop, the ESP32 rig and a phone camera. Keep the same two
pairs for the whole block and for Block 4.2. No phones and no MC: Block 4.2 takes the phone-hosted kills.

**Rules for this block.**
- Do not send `$AS,1`: it starts a native game, a screamer path. Send no `$PHONE` or `$STOP` to a gun in a native
  game. Send no undocumented command (`$IRLVL`, `$INDOOR`, the USB `SETUP`).
- Do not send protocol-15 magnitude 2 from the rig, and do not sweep magnitudes. The hosted 1-39 sweep is done.
- Wait at least 3 s after each tool call.
- A native game drops the BLE link and does not answer `$QUERY` (the 2026-09-10 log). In a native step the only
  instruments are board A, Tony's eyes and ears, and the video.
- A missing word is evidence only when 2b.0's loopback and the ordinary-hit control passed in the same sitting.
- **The receiver.** `source ~/bench-0924.env && $PY mcp/tools/native_capture.py <trial id> COM7 <secs>`. The agent
  starts it in the background, then tells Tony to act. It writes `~/.brx-mcp/ir-captures/<time>-<trial id>.log`
  with host timestamps. Keep RAW on (the tool's default). Do not infer a repeat count from a truncated dump. A pair
  of the same word about 14 ms apart (sensors 0 and 4) is one event until shown otherwise.
- **A rig word.** `source ~/bench-0924.env && cd mcp && $WPY -c "from brx_mcp.irbridge import encode_word as e; print(e(player=7, team=1, damage=9, proto=0))"`
  prints 25 bits; each step names the fields to change. Fire it with
  `source ~/bench-0924.env && $PY -m brx_mcp ir-emit <bits> COM8 [repeat]`, board B 30 cm from the named sensor.
- The arm lines below are `$WPY mcp/tools/armgen.py …` as in Block 2. The agent sends the output with `send_batch`
  to the gun's alias. A is team 1, player 5; B is team 2, player 6.

**2b.0 Versions, loopback, geometry (6 min).**
1. Power-cycle both guns and both headsets. The agent connects each with the MCP tool and sends `$VERSION,*`. Log
   token 1 (the gun) and token 2 (the headset, `hds.*`). The headset string is not mapped to the image names
   v1.27 and v1.34, so log it as it reads.
2. `source ~/bench-0924.env && $PY mcp/tools/loopback.py COM8 COM7 6`, with both guns off the board-B-to-board-A
   axis ([`gotchas.md`](gotchas.md)). **Pass:** both boards alive. **Fail:** stop the block.
3. Put board A beside B's headset, about 1 m from A's line of fire and out of both guns' direct line. Tape the
   mark and photograph it: Block 4.2 reuses it. Log the distances.

**2b.1 Ordinary hit and the hosted kill H0 (F320; 10 min).** Arm both with the silent `<15,0>` row that MC ships,
and friendly fire off, as in an MC match:

```
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 1 5 ar volume=65 friendly_fire=0 '+sir=$SIR,15,0,,28,0,0,1,,*' 2>/dev/null
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 friendly_fire=0 '+sir=$SIR,15,0,,28,0,0,1,,*' 2>/dev/null
```

1. **Control, one ordinary hit.** Capture `2b-H0-ctl` for 30 s. A fires one shot at B's headset. The agent reads
   `get_events` on B and `$LIFE,*` (confirm). **Pass:** one `$HIR`, and one word on board A naming player 5, team
   1. **Fail:** fix the geometry and repeat; do not go on. **Log:** the `$HIR` sensor (token 1) and B's pools.
2. **H0.** Capture `2b-H0` for 90 s. A fires at B's headset until B dies (13 AR hits at stock pools). The agent
   reads `get_events` on both guns once.
- **Log:** every protocol-15 word on board A, as the trial record below asks; any protocol-15 `$HIR` on A or B.
- **Pass or fail (F320):** a (15,2) word within 10 s of the death CONFIRMS that the headset sends the native kill
  word in a hosted game. None, with the control passed, REFUTES it for this run. S57 is not at risk either way: its
  words are magnitudes 21-28 and the phone treats only 21-39 as the bus. A stray (15,2) can at most overwrite the
  phone's `state().beacon`.

**2b.2 Rapid repeated words (F321; 8 min).** Reading: the gun's hit processor has 55, 125, 160 and 250 ms
suppression gates, and the headset a 51-tick transmit gate. B gets one fn-28 row, so no word can kill it:
`source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 -sir=all '+sir=$SIR,0,0,,28,0,0,1,,*' 2>/dev/null`.
The word: player 7, team 1, magnitude 9, protocol 0, at B's front dome. Board A captures each run
(`2b-F321-<run>`) to measure the spacing actually sent.
1. **Control:** 5 single `ir-emit` calls, 3 s apart. **Pass:** 5 `$HIR`.
2. A burst of 5 (`ir-emit <bits> COM8 5 --wait`), three times, 5 s apart.
3. A burst of 10, twice.
4. The control again.
- **Log per run:** the words board A saw, their median spacing, and B's `$HIR` count.
- **Pass or fail:** gating is CONFIRMED if a burst registers fewer `$HIR` than board A saw while both controls read
  5 of 5. The rig has two spacings today: single calls (about 3 s) and its fixed burst spacing (the emitter
  firmware pauses 20 ms between frames; measure the real spacing on board A). A falling ladder at 250, 160, 125 and
  55 ms needs a gap argument on the emitter's `TXN` first. That is desk work, not built.

**2b.3 What one protocol-9 or -10 hit sends (F322; 6 min).** Reading: the headset re-sends a protocol-9 or -10 hit
it receives, with an extra bit set and subtype 0. We ship `$SIR,9,3` and `$SIR,10,0` rows, so one shot could land
twice on a neighbour. Re-arm B stock: `source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 2>/dev/null`
(its table carries both rows). Words at B's front dome, player 7, team 1, magnitude 9, 5 s apart; capture each set
(`2b-F322-<set>`).
1. **Control:** protocol 0, subtype 0, 3 emits. Board A must see one word per emit.
2. Protocol 9, subtype 3, 3 emits.
3. Protocol 10, subtype 0, 3 emits.
4. The control again.
- **Log per emit:** each word board A saw, decoded, with its offset from the first; B's `$HIR`.
- **Pass or fail:** CONFIRMED if a protocol-9 or -10 emit shows a second word that the controls do not. REFUTED if
  every set matches the controls. Re-arm B if its health falls below 20.

**2b.4 fn 36 and 37 on headset domes 1-3 (6 min).** Measured: headset sensor 0 scales fn 36 and 37 with `$GSET`
t7, and the gun body (sensor 4) applies ×1. Reading: sources 0 to 3 all scale. B gets one fn-37 row at t7 = 100:
`source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 crit_modifier=100 -sir=all '+sir=$SIR,0,0,,37,0,0,1,,*' 2>/dev/null`.
The word: protocol 0, player 7, team 1, magnitude 9. Tony aims board B at one sensor and covers the others with his
hand. The agent reads `$LIFE,*` (confirm) before and after each hit.
1. Sensor 0 (control, expect 27), then the gun body (control, expect 9).
2. One hit on each of the other three domes. Re-arm B whenever its pools total less than 30.
3. Sensor 0 again.
4. Optional: the same on fn 36 (expect 13 on a scaling sensor).
- **Log per hit:** `$HIR` token 1 (the sensor, which is the truth, not Tony's aim) and the pool drop.
- **Pass or fail:** a dome that reads 27 CONFIRMS the reading for that source; 9 REFUTES it. A sensor-0 control
  that does not read 27 voids the step.

**2b.5 fn 18 and fn 22 against an enemy (6 min).** Measured: fn 20 against an enemy takes armour only. Reading: fn
18 (a shield grant) and fn 22 (an armour grant) also have opposing-team subtraction branches, unmeasured. Arm B
with one row per function, re-arming for each:
`source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 shield=70 -sir=all '+sir=$SIR,0,0,,<fn>,0,0,1,,*' 2>/dev/null`.
The word: protocol 0, player 7, team 1 (an enemy of B), magnitude 9. Run fn 20 (control), fn 22, fn 18, then fn 20
again. For each: `$LIFE,*`, two hits, `$LIFE,*`.
- **Log:** which pool moves, and by how much.
- **Pass or fail:** each fn 20 control takes 9 armour a hit, or the step is void. For fn 22 and fn 18, the pool
  that moves is the result. No change, with a live control, means that branch is not reached from an enemy word.
  If B's shield reads 0 before fn 18, the fn 18 half is void (the shield pool may need activating, P16).

**2b.6 The headset's stored-ID compare (optional; 6 min).** Reading, headset v1.34 only: the hit path compares an
incoming player id with a stored identity slot; v1.27 does not. The effect is unknown. The stored slot can be
changed only through the USB `SETUP` re-pair, so vary the incoming id instead. Re-arm B stock. Words at B's front
dome, protocol 0, team 1, magnitude 9, 5 each, 3 s apart: player 7 (A), then player 6, B's own id (B), then
player 7 (A again). If Tony has the programming cable to hand, read B's device record with
`source ~/bench-0924.env && $PY -m brx_mcp usb-query <port>` and add 5 words with its `PlayerID`.
- **Log per set:** the `$HIR` count, the pool drop, and the headset's LEDs and sounds.
- **Pass or fail:** a difference in B that the second A set does not repeat CONFIRMS that the incoming id matters;
  log it against 2b.0's headset string. No difference is INCONCLUSIVE: the slot may not hold the hosted id.

**2b.7 Native TDM: teams and player ids (N0; 8 min).** No page says how to give a gun a player id in a game
started from the menu. The team is known: LEFT and RIGHT on the menu pick the mode and the team. One native
capture (2026-09-04) decoded a killer id of 42, and the community notes say offline ids are random. So this step
reads the ids before any trial.
1. Disconnect the MCP session from both guns. Power-cycle both guns and headsets. Tony sets both to Death Match
   (the red menu mode, Alpha against Bravo), A on one team and B on the other. He sets the respawn to its longest
   value: a native respawn timer revived a gun inside a 2026-09-04 trial. Log the settings, then start both.
2. Capture `2b-N0` for 60 s. Tony fires 3 shots from each gun at board A from 1 m, A first.
- **Log:** each gun's decoded player id and team.
- **Stop rule:** if the two guns decode the same player id or the same team, or either gun gives no clean word in
  3 shots, do not run 2b.8. Log the ids as the finding and go to 2b.9. If an id changes after a power cycle,
  repeat this read after every power cycle.

**2b.8 Native kills N1-N3 (18 min).** Codex's trials. Before each, Tony returns both guns to the menu (LEFT+RIGHT
held 5 s) and starts a fresh Death Match, so B starts each trial with full pools.
1. **Control, before N1 only:** capture; A fires one shot at B's headset. **Pass:** one word on board A naming A's
   id from 2b.7, and B's headset flashes.
2. For each trial: capture `2b-N<k>` for 60 s. Film B's headset and board A from 5 s before the death to 15 s after.
   A fires at B's headset until B dies. Tony types `1` at B's death sound, and the agent notes the time.
- **Log per trial:** the trial record row; B's headset LEDs and sounds, and A's. The gun reading says a killer's
  gun credits a (15,2) that names its own team and id, so listen for a kill-confirm on A.
- **Pass or fail:** see the result rule under the trial record.

**2b.9 Native Survival and FFA (optional; 14 min).** Readings: Survival (mode 5) uses teams 0 and 3, and its death
branch moves the dead player to team 3. FFA puts every gun on team 1 with same-team hits on. A v1.34 headset with
stored mode 5 sets its LED colour at once on a fatal hit; the code does not tie that headset value to the gun's
mode 5.
1. **`$QUERY` from the menu.** Tony sets B to Survival (green) and stops at the ready screen. The agent uses the
   plain `connect` (not `identify` or diagnostics, which send `$STOP` and `$PHONE`) and sends `$QUERY,*` with
   confirm. **Stop rule:** no reply in 3 s, or no connect: log "no reply" and disconnect; send nothing else. If the
   gun leaves the ready screen on connect, the step is void. A reply: log it whole and parse it with
   `parse_query_dump`. Repeat for FFA (white).
2. **The Survival death LED.** Power-cycle. Both guns on Survival, A infected (team 3 in the reading), B human.
   Start. Capture `2b-N-surv` and film B's headset. A kills B.
- **Log:** B's headset LED from the death to 15 s after, every word on board A, and B's team colour afterwards.
- **Pass or fail:** the control is the TDM death on the 2b.8 video. A different LED CONFIRMS a mode-5 death branch
  on this headset. The same LED is INCONCLUSIVE, because the code does not link the two mode values.

**2b.10 Restore (4 min, always).** End any native game (LEFT+RIGHT held 5 s). Power-cycle both guns and headsets.
Re-arm both stock (`armgen.py 1 5 ar volume=65` and `armgen.py 2 6 ar volume=65`), fire one control shot from A at
B and confirm the `$HIR`. Then power-cycle both again and disconnect the MCP session.

**Not scheduled: F293's GPIO probe.** Reading: the v4.32 loop pulses GPIO 3 or 31 low for about 500 ms on a 6 s
timer, and a pulse before each joint drop would show a gun-driven reset. Probing it means opening a gun. That is
Tony's decision, so it is listed here and not scheduled.

**Trial record.** One row per trial. Raw capture files stay out of the prose log; the dated entry names them by
trial id.

| trial | mode, start | A and B ids, teams | pools before and after | fatal `$HIR` sensor, shooter | protocol-15 words and offsets | gun and headset feedback | controls valid? |
|---|---|---|---|---|---|---|---|
| H0 | hosted, laptop (2b.1) | | | | | | |
| N1 | native Death Match, menu (2b.8) | | | | | | |
| N2 | native Death Match, menu | | | | | | |
| N3 | native Death Match, menu | | | | | | |
| H1 | hosted, phone and MC (Block 4.2, death 1) | | | | | | |
| H2 | hosted, phone and MC (Block 4.2, death 2) | | | | | | |
| H3 | hosted, phone and MC (Block 4.2, death 3) | | | | | | |

For each protocol-15 word, log the full 25 bits, the protocol, subtype, id, team and magnitude, which receiver saw
it, and its offset from the fatal `$HIR` and `$HP,0,0,0` (hosted) or from Tony's `1` (native).

**Result rule.** N1-N3 here and H1-H3 in Block 4.2 make three native and hosted pairs, with the same guns, headsets
and receiver mark in one sitting. H0 is the hosted control taken beside the native trials. Three clean pairs give a
result; two are provisional. Record a missing control or an unfinished trial as open, not as a negative. If 2b runs
in a sitting without Block 4, take H1-H3 here instead, as repeats of 2b.1 step 2, and alternate them with the
native trials: N1, H1, N2, H2, N3, H3.

## Block 3: trigger order and fire intervals (20 min)

One gun on the MCP session, no phone.

**3.1 F308: does every release reach the host, in order?** The recoil burst reset depends on `$BUT,0,0`.
1. The agent connects and sends `$WPY mcp/tools/armgen.py 1 5 ar volume=65` with `send_batch`.
2. **Control:** one single shot. `get_events` must show one `$BUT,0,1`, one `$ALCD`, one `$BUT,0,0`, in that order.
3. Five bursts, the trigger held about 2 s each, a full release and a reload between bursts. Type `1`.
4. The agent reads `get_events` once, with `max_events=2000`.
- **Log:** the count of `$BUT,0,1` and `$BUT,0,0`, and per burst the seq of its last `$ALCD` against its `$BUT,0,0`.
- **Pass or fail:** 5 presses and 5 releases, and each release comes after its burst's last `$ALCD`.

**3.2 Fire intervals: Shotgun, Desert Eagle, Burst Rifle (F291, F308; 10 min).** For each weapon, print its compiled
frame and send it with `send` on the armed gun (it replaces slot 0 and refills the magazine):
`source ~/bench-0924.env && cd mcp && $WPY -c "from brx_mcp.mc.compile import WeaponCatalog as C; print(C().resolve('<id>', 0)); print(C().fire_ms('<id>'))"`
with `<id>` = `shotgun`, then `deagle`, then `burst_rifle`. Tony holds the trigger for about 3 s (the Deagle and
Shotgun fire one round per pull, so he pulls as fast as he can). The agent takes the `t_ms` gap between consecutive
`$ALCD` frames from `get_events`.
- **Pass or fail:** Shotgun and Desert Eagle: the shortest gap matches the printed `fire_ms` (700 ms on 2026-09-24),
  within 50 ms. Burst Rifle: gaps inside a burst match its `fire_ms` (75), and the gap between bursts matches the
  frame's t23 (550 ms), within 50 ms.
- **Log:** each weapon's gaps.

## Block 4: the 0.4.6 loop and one real match (80 min)

Two guns, both Pixels, Mission Control. Start MC from `mcp/` with the laptop's LAN address from `ipconfig`:
`setsid nohup ../.venv/bin/python -m brx_mcp.mc --advertise <laptop LAN address> --bench-volume`. Restart MC
between matches, never while a match is ARMED or LIVE.

**4.0 The 0.4.6 build, the whole loop (20 min).** Install the held 0.4.6 APK on both phones (brx3 holds it,
`~/brx3-release`; `adb -s <phone> install -r <apk>`) and log `window.brx.APP_VER`. First launch on each phone, once in
day and once in night mode. Then one short match through MC on real guns: lobby, arm, live, a death with its death
screen and callouts, and the recap on MC and both phones.
- **Pass or fail:** every screen appears in order with no stuck or blank state, on both phones and in both modes.
  Log each defect with a screenshot. This is the 0.4.6 go or no-go.

**4.1 The new respawn rules.** A timed respawn: no protection by default, weapon live
0.5 s after the countdown. A station respawn: 2 s visible protection, trigger live at once, white shield blink.
- **Pass or fail:** each behaviour as described, seen on both phones.

**4.2 S56, the hit relay.** Only player A shoots; player B does not fire. A kills B three times. About 3 s after each
death (the relay's grace), the agent reads both phones:
`JSON.stringify({life: window.brx.engine.state().life, last: window.brx.engine.state().lastLife})`
- **Log per death:** in B's `lastLife`, the `taken[]` row whose `num` is A's (its `dmg`), and `takenTotal`; in A's
  `life`, the change in `dealtTotal` since the previous read, and `dealtPartial`.
- **Pass or fail:** the two numbers match for each death. The relay is best-effort, so log a mismatch with
  PARTIAL set as a relay gap, and one without it as a bug.
- **The same three kills are Block 2b's hosted trials H1, H2 and H3 (F320).** Use 2b's two guns and headsets, and
  put board A on 2b.0's taped mark beside B's headset. Before each kill the agent starts
  `source ~/bench-0924.env && $PY mcp/tools/native_capture.py 2b-H<k> COM7 90` in the background. Log every
  protocol-15 word in 2b's trial record, and any protocol-15 `$HIR` in either ring (MC ships the silent `<15,0>`
  row). 2b.8's N1-N3 are their native pairs; 2b.1's H0 is the laptop-hosted control. If Block 2b did not run in
  this sitting, still capture: the words count for F320, but not as pairs.

**4.3 F308, the Assault Rifle ladder.** The numbers are the `assault_rifle` row of the recoil table in
[`spec/node.md`](spec/node.md) §3.15. On 2026-09-23 it read degraded 70 from round 6, heavy 40 from round 8, and
crisp again after 600 ms of quiet; brx1's balance lane is still moving it, so read the row again on the day.
Player A has the AR. Tony fires a burst of 5, pauses, then holds the trigger to about round 10, then waits 1 s. Then
the agent reads A's ring (`window.brx.link.frames`):
- **Log:** the round number (count the `$ALCD` frames) and the `t` of each `tx` `$TMP` frame, with its token 4. Token
  4 is a modifier, the rung minus 100, so today's row reads -30 (degraded), -60 (heavy) and 0 (crisp).
- **Pass or fail:** no accuracy write during the burst of 5; the degraded modifier after the `after_shots` round;
  the heavy modifier after the `after_heavy` round; 0 again about `settle_ms` after the last `$ALCD`. Log Tony's word
  on the feel too.

**4.4 The Charge Rifle tap (F291).** A on the Charge Rifle: one charged shot on B, then three taps. Read B's ring.
- **Pass or fail:** `$HIR` magnitude 70 for the charge and 16 for each tap (`$WEAP` t37, `3759cd68`). Control: an AR
  hit on B reads its own magnitude, 8.

**4.5 F292, the Toxin Rifle.** Play this on the Hardcore preset (45/0/0), so a tick has no armour or shield to
spill into.
- **Listen:** A hits B once with the Toxin Rifle. Expect H23 on the hit, then V4G "Cough!" on each tick. Control:
  an AR hit plays neither.
- **Wire:** read B's ring. Each tick's `$HP` echo must move health down by exactly the tick amount (4).
- **Lethal tick:** A shoots B with the AR until B's health is 20 or less, then one Toxin hit; let the ticks kill.
  MC's recap must book a credited `death` with `dot: true`.
- **Pass or fail:** all three.

**4.6 F298, the Shields preset.** One match on Shields (45/0/105). Watch the recharge cues and the pool readout on
both HUDs.

**4.7 F296 and full screen.** Does the slowed `$HLOOP,1,2500` rearm read as
"down" and not as a hit? Does the Android status bar stay hidden during play on both phones?

**4.8 F309 (`57d6e0d7`; skip if Block 0's check is 0, and the phones need a build that includes it).** brx3's four checks. A phone claims
`cellular` only while the network its socket bound on and the current network are both cellular, so after a
Wi-Fi-to-mobile switch it counts only once it has rebound. The expected green chip, as a picture:
`?mock&allgreen=1&fullcoverage=1` in the MC console.
1. A Pixel with Wi-Fi and mobile data both on: what does `transport` report, `wifi` or `cellular`?
2. Both phones on cellular through the tunnel: the chip reads "Internet: 2 of 2 phones · 2 on cellular", in green.
3. One phone on the field Wi-Fi through the tunnel: the chip drops to zones, in grey.
4. Mobile data off mid-lobby: the count falls within one heartbeat (about 2 s).
5. **F311, brx-net (`709aec07`).** Field Wi-Fi with no internet, mobile data on, tunnel off: the phone binds to MC
   over the LAN and stays bound for a full match, the LAN sweep finds MC, and a Wi-Fi drop and rejoin mid-match
   redials. The Block 0 build from `main` carries the native plugin; the published 0.4.5 does not.

**4.9 The heavies on Shields (F310, Tony 2026-09-23).** One match on the Shields preset (45/0/105), with the Rocket
Launcher and the Rail Gun as pickups.
- **Rail Gun:** wait until the victim's shield ring is full (it starts empty and refills in about 10 s), then one
  hit. Read the victim's ring: `$HIR` magnitude 149, and the player left on 1 health with the shield empty.
  Control: the same hit on the Standard preset kills.
- **Rocket Launcher:** one hit at arm's length. Both words land (`$HIR` 115 and 35) and the player dies. Log the
  order of the two `$HIR` frames (the node pairs them assuming the 115 comes first). Then one hit at about 10 m, as
  an observation only: the headset word's reach is unmeasured (F275), so log whether the 35 arrived.
- **Pass or fail:** the rail and the close rocket as described. Two unmeasured facts ride on this: a 149
  magnitude is the largest this project has sent (a clamp shows in `$HIR`), and damage spilling from the shield
  into health within one hit is documented but not measured (if the rail leaves 45 health, the gun stops at the
  shield layer).

**4.10 Telling two magnitude-9 weapons apart (F315).** Player A carries the Assault Rifle and the Energy Rifle.
- **Control, flag off:** A hits B once with each. B's hit tag and death screen read "ASSAULT RIFLE / ENERGY RIFLE"
  for both: they share cell (0,0) and magnitude 9.
- **Flag on:** restart MC with `--distinct-weapon-cells`, re-arm, and repeat. Read B's ring: the Energy Rifle's
  `$HIR` carries subtype 2 (token 7) and still magnitude 9, and B's pool drops by the same 9 as an AR hit. B's tag
  names each weapon alone.
- **Also, flag off or on:** A with the Assault Rifle and the USP-S. The tag names each alone already (cells (0,0)
  and (0,3)), with no flag.
- **Pass or fail:** a (0,2) word registers, echoes subtype 2 and lands its full magnitude. Only then may the flag
  default on; if it does not register, the flag stays off and "A / B" stays for that pair.

## Block 5: the headset word's range, F275 (60 min, outdoor)

The procedure is the F275 row: an F231-style shaded ladder against `t13`, with the same rig, the same shading fix
and the discard-the-first-two-pulls control (F232). The shooter carries the compiled Shotgun with `t13` edited
(doc token 13 is comma field 14; today it is 100, the stock value and the control):

```
source ~/bench-0924.env && cd mcp && $WPY -c "from brx_mcp.mc.compile import WeaponCatalog as C; f = C().resolve('shotgun', 0).split(','); f[14] = '<t13>'; print(','.join(f))"
```

Arm the shooter with `armgen.py 1 5 ar volume=65`, then send that frame; arm the victim with `armgen.py 2 6 ar
volume=65`. The compiled Shotgun also carries t42 = 100, which takes over from t13 only when the `$GSET` IR level is
not zero; armgen sends `$GSET` t2 = 0, so t13 is the token under test. Fire at the victim at 5 m and at 25 m at each
`t13` value, with 100 first and last. `t13` is a carrier frequency, 140 Hz per step (levers §20), so read the result
as a frequency ladder.
- **Log:** per `t13` value and distance: pulls fired, and headset words (magnitude 20) received on the victim.
- **Pass or fail:** a `t13` value where the word arrives at 5 m and not at 25 m. That value is the edge of the
  close-range balance band. The closing 100 must reproduce the opening one.

## Block 6: super indoor, in Tony's house (S48, F198; 60 min, plus 15 optional)

Tony's definition (S48, 2026-09-23): "super indoor" is a suburban house with light eggshell paint, which reflects IR
strongly, so a shot at the ceiling or a wall bounces and still hits. **Acceptance:** at least 95% of aimed shots
across a room hit, and at most 5% of deliberate bounce shots hit. The grid is `$WEAP` t2 on the shooter against
`$GSET` t2 on the victim. brx5 is reading the firmware for more IR controls; any it finds join this grid.

⚠️ **What t2 does.** `$WEAP` t2 sets the emitter's carrier frequency, not its power (S48, F231, levers §20). A low
value detunes the word away from the receiver's band, which costs reception at every distance. The sweep can still
work: a bounced word arrives weaker than an aimed one, so a detune may lose the bounce first. F231's outdoor ladder
put the transition between about 13 and 50 (13 landed 3 of 8 at 15 m, 25 and 32 landed 15 of 32 at 10 m, 50 landed
21 of 23), and 5 landed nothing. So the grid uses 100 (stock, the control), 50, 32, 25 and 19. `$GSET` t2 = 1 made
a receiver "nearly deaf" in the 2026-09-13 field (F198); that is why it is the other axis.

**6.0 Optional, needs a scope: carrier and duty (S48; 15 min).** Reading (v4.32 and v2.08b emitters): the carrier
is `38000 - 125 × (100 - t2)` Hz below t2 = 100, and the gun's indoor or outdoor mode picks one of two fixed output
settings (the S48 row gives about 20% against 38% duty). If that holds, the only emitted-power lever is the
physical mode. Put an IR photodiode on the scope probe, 5 cm in front of the shooter's emitter. Do not use a
VS1838B: it demodulates the carrier away. Arm the shooter stock (`armgen.py 1 5 ar volume=65`, t2 = 100).
1. **A:** the gun powered on in outdoor mode (ALT at power-on, [`manual/hardware.md`](manual/hardware.md)). Three
   single shots. Log the carrier frequency and the duty.
2. **B:** power-cycle into indoor mode, re-arm, three shots.
3. **A again:** outdoor mode, three shots. It must reproduce step 1.
4. In outdoor mode, re-arm with `$GSET` t2 = 1 (the victim `sed` below, on the shooter's arm), three shots.
- **Pass or fail:** a duty change between A and B CONFIRMS a physical power lever. A duty change in step 4 means
  `$GSET` t2 reaches it over BLE, which S48 could build on. Send no `$IRLVL` or `$INDOOR`: neither is on the
  known-safe list. Restore: power-cycle the shooter, then arm it for the grid.

**Setup.** The laptop's MCP session holds both guns. Use each gun's usual alias (a `connect` alias is saved per
address); below, "shooter" and "victim" name them. Mark a firing spot across
the room from the victim (log the distance), and pick a bounce point on the ceiling or a wall well off the victim
(log where). The victim's `$SIR` table is one fn-28 row, so every hit sends `$HIR` and no pool changes: 400 shots
cannot kill it. Every cell is a full re-arm, which also restores the magazine (a `$WEAP` write resets it).

Victim, `$GSET` t2 = `<s>` (0 or 1); the `sed` edits token 2 of the arm's `$GSET` frame:

```
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 6 ar volume=65 -sir=all '+sir=$SIR,0,0,,28,0,0,1,,*' 2>/dev/null | sed -E 's/^(\$GSET,[^,]*,)0,/\1<s>,/'
```

Shooter, `$WEAP` t2 = `<p>`:

```
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 1 5 ar volume=65 t2=<p> 2>/dev/null
```

The agent sends each printed sequence with `send_batch` to its alias. Check the victim's `$GSET` line reads
`$GSET,1,<s>,…` and the shooter's `$WEAP,0` line reads `$WEAP,0,,<p>,…` before any shot.

**Per cell:** arm both guns. Tony fires 20 aimed shots at the victim's headset from the mark and types `1`; then 20
shots at the bounce point and types `1`. Tony reloads between the two windows (40 shots
is more than the 32-round magazine). The agent counts the victim's `$HIR` frames in each window from `get_events`
on the victim, and the shooter's `$ALCD` count as shots fired.

| order | shooter t2 | victim `$GSET` t2 | why |
|---|---|---|---|
| 1 | 100 | 0 | opening control: today's shipped pair; expect the bounce problem |
| 2-5 | 50, 32, 25, 19 | 0 | the detune ladder at normal sensitivity |
| 6 | 100 | 1 | sensitivity alone |
| 7-10 | 50, 32, 25, 19 | 1 | both levers together |
| 11 | 100 | 0 | closing control: must reproduce cell 1, or every cell between is void |

- **Log per cell:** shots fired and hits, aimed and bounce. Also any hit that Tony did not aim anywhere near.
- **Pass:** a cell passes with at least 19 of 20 aimed hits and at most 1 of 20 bounce hits. Re-run a passing cell
  once more before it counts, then log the winning pair for S48's `house` venue.
- **Stop early** if a column's aimed hits fall below 19 of 20: lower values in that column only get worse.

**Restore (always, even after a void run).** Re-arm both guns stock (`armgen.py 1 5 ar volume=65` and
`armgen.py 2 6 ar volume=65`, no edits), fire one aimed shot and confirm it hits. Then power-cycle both guns and
both headsets, so no gun is left detuned or deaf.

## Block 7: the IR callout bus (S57; 55 min)

Steps from brx5 for brx4's design (S57): a phone that knows a game event has its gun broadcast one `$IRTX` word,
and other guns hear it. Two guns on the MCP session; the ESP32 receiver (`native_capture.py`) faces A's headset
domes as the emit control. Already proven on 2026-09-18, so not re-run: a dead gun forwards a host `$IRTX`, and
magnitudes 1-39 are silent with player 0.

**Arm.** Sender A on team 2, receiver B on team 1, both with the `<15,0>` fn-28 row and friendly fire off:

```
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 2 5 ar volume=65 friendly_fire=0 '+sir=$SIR,15,0,,28,0,0,1,,*' 2>/dev/null
source ~/bench-0924.env && $WPY mcp/tools/armgen.py 1 6 ar volume=65 friendly_fire=0 '+sir=$SIR,15,0,,28,0,0,1,,*' 2>/dev/null
```

The word, sent from A with `send`: `$IRTX,100,15,<pid>,<field4>,<mag>,0,0,100,1,,0,*`. Log every `$HIR` on both
guns with its time and first field (the sensor).

1. **Self-report, live sender.** 5 words from live A, magnitude 21, 3 s apart. Log B's `$HIR` count, and whether A
   reports its own word.
2. **Dead sender.** Kill A with `$BUMP,-9999,1,1,1,,*`; `$LIFE,*` must answer `$HP,0,0,0`. Repeat step 1.
   Pass: B gets 5 of 5. Log any self-report on A.
3. **Dead receiver.** Revive A (`$LIFE,30,0,0,1,*`) and kill B. 5 words: expect none, since fn 28 is ignored on a
   dead gun. Then set B's row to fn 34 (`$SIR,15,0,,34,,,,,*`) and send 5 more. Pass: B sends `$HIR` while `$HP`
   stays 0,0,0 and B stays dead; read `$LIFE,*` after each word, and log B's headset LEDs too. If fn 34 registers
   silently and B stays dead, one row change lets dead players hear callouts. Restore fn 28 and revive B.
4. **Team gating.** B live on team 1: 5 words each with field 4 = 0, 1, 2 and 3. Move B to team 2 and repeat. Log
   which field-4 values register on each team. Field 4 = 2 is the key value: if every team hears a team-2 word, S57
   uses a constant 2, so log it against both receiver teams as carefully as 0 and 3. Watch for the team-2 neutral trap.
5. **Duplicates.** 20 single words 3 s apart, with B at 2 m and again at 0.5 m, domes facing. Per word: how many
   `$HIR` arrive, from which sensors, the gap from first to last, and the ESP32's count. Pass for brx4's 600 ms
   de-duplication: every duplicate lands within 600 ms of its first.
6. **Native reaction.** The exact frame with a non-zero player id, magnitudes 21 to 28, one each. Listen for sound,
   watch B's headset LED, and compare `$LIFE,*` before and after.
7. **Range.** With A dead, 10 words at 3, 10, 20 and 30 m, domes toward B. Then 10 live shots from A's barrel on a
   normal weapon (t2 100) at the same marks; drop the first two live shots (F232). One indoor run from the next
   room with no line of sight: a bounced callout is welcome, unlike a bounced shot.
8. **The capturing team hears its own capture (F312).** A grenade in Hill mode, both guns on team 1 with friendly
   fire off and the `<15,0>` fn-28 row. Capture it with A (team 1). Log whether A and B report the magnitude-50 word
   and the next magnitude-8 beacons. Then move B to team 2 and repeat, capturing with A. Pass for the S57 design: the
   team-2 receiver reports both; the question is whether team 1 reports anything. If team 1 is deaf, set A's (the
   capturer's) row to fn 34 (step 3), capture again, and log whether A registers it without changing A's pool.
   Or run MC with `--bench-capture-row 34`: every live table then carries `$SIR,15,0,,34,,,,,*` (the class-sound
   takes too), the head stays the fn-28 twin, and a banner shows the flag is on.
9. **Magnitudes 40 to 43 (CTF reserve).** The step-6 frame with magnitudes 40, 41, 42 and 43. Listen, watch B's LEDs,
   and compare `$LIFE,*` before and after. Silent means CTF's drop and return codes can live there.
10. **A Stick hill's capture word.** Only if a StickS3 is flashed: HILL mode, capture it with A. Log the Stick's
    `OWNER` line and whether B reports one magnitude-50 word within 100 ms of it (the phone path needs nothing more).

**Restore.** Re-arm both guns stock (`armgen.py 1 5 ar volume=65` and `armgen.py 2 6 ar volume=65`), fire one
control shot, then power-cycle both guns and headsets. Record the results against S57.

## Close

The three writes in [`README.md`](README.md): one experiment-log entry with every run, including controls and
nulls; one FOLLOWUPS diff; the HANDOFF lane update. Mark each claim CONFIRMED, REFUTED or INCONCLUSIVE. Strike the
steps that ran from this sheet and from [`bench-plan.md`](bench-plan.md).
