# Phone HUD v2 — Claude Design export (2026-08-25)

Visual source of truth for the per-player node app (`app/`): **landscape, rail-mounted**, ten screen
states incl. full-screen moments (T-MINUS, KILL CONFIRMED, DOWN, REDEPLOY), camera look-through and
blackout. Demo gun ids scrubbed to `GUN-A…`. Behaviour truth is `docs/spec/node.md` (A6); where the
design and the older portrait layout in node.md §4.1 differ, **this design wins on layout** — the
honesty rule ("— MC" / ✓MC), blackout, mount + foreground requirements are unchanged.
