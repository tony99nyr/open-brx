# Mission Control — Claude Design export (2026-08-25)

The `Mission-Control.dc.html` prototype + its handoff `README.md` are the **visual source of truth**
for `webapp/mc/` (recreated pixel-close in React). Demo gun ids were scrubbed to `GUN-A…H` (real
sticker ids never enter the repo). Iterate visuals in Claude Design from this export, re-export,
and diff against `webapp/mc/src/screens/*` — see memory "Mission Control design workflow".
Behaviour/data truth is `docs/spec/mission-control.md` + `mcp/brx_mcp/mc/API.md`, not the prototype.
