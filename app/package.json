{
  "name": "brx-companion-app",
  "version": "0.1.0",
  "private": true,
  "description": "BRX native BLE app (Capacitor) \u2014 Android + iOS from one codebase",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=iife --outfile=www/app.js",
    "sync": "npm run build && npx cap sync",
    "ios:setup": "npm run build && ./scripts/ios-setup.sh",
    "ios:open": "npx cap open ios",
    "android:setup": "npm run build && (test -d android || npx cap add android) && npx cap sync android"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^8.3.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
