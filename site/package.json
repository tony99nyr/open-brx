{
  "name": "open-brx-site",
  "private": true,
  "type": "module",
  "description": "Static generator for the Open BRX website: renders docs/manual/*.md into webapp/ (Cloudflare assets).",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "test": "node build.mjs && playwright test",
    "test:only": "playwright test"
  },
  "devDependencies": {
    "marked": "^18.0.11",
    "playwright": "^1.62.1",
    "@playwright/test": "^1.62.1"
  }
}
