{
  "name": "open-brx",
  "private": true,
  "description": "Root build entry. Cloudflare Workers Builds runs `npm run build` to render docs/platform/*.md and docs/manual/*.md into webapp/ before deploying.",
  "scripts": {
    "build": "cd site && npm ci --no-audit --no-fund && node build.mjs",
    "test": "cd site && npm test",
    "build:ci": "cd site && npm ci --omit=dev --no-audit --no-fund && node build.mjs"
  }
}
